# 0x00 Hello World

This project contains a script `0-preprocessor` that runs a C file through the C preprocessor.
The script takes a C source file stored in the environment variable $CFILE and outputs the result into a file called `c`.
