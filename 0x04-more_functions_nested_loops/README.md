# 0x04-more_functions_nested_loops

## 0-isupper
Function that checks for uppercase character. Returns 1 if c is uppercase, 0 otherwise.

## 1-isdigit
Function that checks for a digit (0 through 9). Returns 1 if c is a digit, 0 otherwise.
## 2-mul
Function that multiplies two integers. Returns the product of a and b.
